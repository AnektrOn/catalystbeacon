/**
 * Single Toolbox tab — delegates to Robust implementation with optional layout.
 * Use layout="mobile" | "compact" | "default" for responsive behavior.
 */
export { default } from './ToolboxTabRobust';
