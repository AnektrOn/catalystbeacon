# ⚡ Synchroniser Stripe en 1 Minute

## 🎯 En Une Phrase

**Copiez le fichier `sync_stripe_subscriptions.sql` dans Supabase SQL Editor, exécutez-le, puis utilisez `SELECT * FROM sync_all_subscriptions_from_stripe();` pour synchroniser.**

---

## 📋 Les 3 Actions

### 1️⃣ Copier
Ouvrez `supabase/migrations/sync_stripe_subscriptions.sql` et copiez TOUT

### 2️⃣ Coller
Dans Supabase → SQL Editor → Collez et cliquez **Run**

### 3️⃣ Utiliser
Tapez `SELECT * FROM sync_all_subscriptions_from_stripe();` et cliquez **Run**

---

## ✅ C'est Tout !

Maintenant vos abonnements Stripe sont dans votre base de données.

---

**Besoin de plus de détails ?** → `STRIPE_SYNC_VISUAL_GUIDE.md`
